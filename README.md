# Order Manager API
API выполняющее CRUD операции для управления заказами, реализованное на Jakarta EE и Tomcat 11.
Позволяет:
1. Создать заказ (POST /orders)
2. Получить заказ по ID (GET /orders?id={id})
3. Обновление заказа (PUT /orders?id={id})
4. Удаление заказа (DELETE /orders?id={id})

